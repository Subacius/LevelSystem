using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerAddNew : MonoBehaviour
{

    [SerializeField] private GameObject pfGameObject;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // public void AddNewPlayer() {
    //     // pfGameObject.GetComponent<PlayerLevel>().Reset();
    //     // pfGameObject.GetComponent<PlayerLevel>().GenerateID();

    //     GameObject newObject = Instantiate(pfGameObject, new Vector3 (-3, 0 , 0), Quaternion.identity);
    //     newObject.GetComponent<PlayerLevel>().Reset();
    //     newObject.GetComponent<PlayerLevel>().GenerateID();



    // }
}
